package ru.job4j.ood.lsp;
// Смотрите класс CalculationFerma
public class CalculationFermaInAnotherRegion extends CalculationFerma {
    private boolean calculation() {
        return g < ((N / 2) * A * l * (l / 2));
    }
}
